<%- include('partials/head') %>
<%- include('partials/header/header') %>
<%- include('partials/header/waves-header') %>

<section id="details" class="details">
    <div class="object">
        <div class="image">
            <img src="<%= data.webImage ? data.webImage.url : './img/placeholder.png' %>">
        </div>
        <div class="info-container">
            <input id="tab-1" type="radio" name="tabs"/>
            <input id="tab-2" type="radio" name="tabs"/>
            <input id="tab-3" type="radio" name="tabs"/>
            <div class="tabs">
                <label for="tab-1">Object Nr.</label>
                <label for="tab-2">Presentatie datum</label>
                <label for="tab-3">Kleuren</label>
            </div>
            <div class="content">
                <div class="tab">
                    <div>
                        <span><%= data.objectNumber %></span>
                    </div>
                </div>
                <div class="tab">
                    <div>
                        <span><%= data.dating.presentingDate %></span>
                    </div>
                </div>
                <div class="tab">
                    <div class="colors">
                        <% if(data.colors.length !== 0) {%>
                            <ul>
                                <% data.colors.map(colors => { %>
                                    <li class="hex-color" style="background-color: <%= colors.hex %>"><%= colors.hex %></li>
                                <% }).join('') %>
                            </ul>
                        <%} else {%>
                            <span class="not-defined">niet bekend</span>
                        <%}%>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<%- include('partials/footer') %>
